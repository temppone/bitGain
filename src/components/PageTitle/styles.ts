import styled from 'styled-components';

export const PageTitleContainer = styled.div`
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    padding: 1rem;
`;

export const PageTitleName = styled.h1`
    font-size: 2rem;
`;

export const PageTitleSalutation = styled.p`
    font-size: 1.2rem;
`;
